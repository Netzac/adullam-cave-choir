Read CLAUDE.md for full project context.

Find every component still using placeholder. Replace with image_url fetched from Supabase storage. Use next/image with proper props. Add fallback handling for null values.

Run npm run typecheck after. List every file changed.